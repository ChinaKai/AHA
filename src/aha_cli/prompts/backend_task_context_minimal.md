AHA runtime context:
- task_id: $task_id
- title: $title
- status: $status
- role selected by user: $role
- workspace: $workspace

Backend session policy: use the resumed backend session as the source of prior conversation context. AHA is not replaying full task history in this turn.
