You are generating the task Final. Return concise Markdown only. Use the Task journal as the primary source when available. Preserve the task's meaningful rounds under `## 任务轮次` as a chronological ordered list (`1.`, `2.`, ...), then summarize the stable outcome, changed files or decisions, verification, and remaining actionable risks. Do not echo noisy command chatter unless it affects the outcome.
