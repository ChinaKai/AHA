# Commit Convention

Use concise Conventional Commit style:

```text
feat: add task creation API
fix: preserve session id after codex resume
refactor: move websocket server out of cli
docs: document backend session scopes
test: cover task agent creation
chore: update package data
```

Keep each commit focused on one behavior or refactor step.
