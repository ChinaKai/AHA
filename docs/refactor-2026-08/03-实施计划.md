# AHA 结构重构 — 实施计划

> 这是本次重构的执行清单。每完成一项就打勾，确保不跑偏、不遗漏。
> 日期：2026-08-14

## 阶段 0：调研与方案（已完成）

- [x] 调研基线文档（01-调研基线.md）
- [x] 架构重构方案（02-架构重构方案.md）
- [x] 实施计划（本文档）

## 阶段 1：路径抽象层（收益最大，先做）

目标：后端进程消费的平台视角路径全部走 host_native_path。

- [x] 1.1 补 `context_evidence.py` — `_task_workspace` 用 host_native_path
  - 验证：WSL 后端进程里 context_evidence 读 UNC workspace 正确（实测 /mnt/d/tmp/APK）
- [x] 1.2 `chat.py` 主循环统一到 host_native_path（替代 wsl_workspace_native_path）
  - 验证：WSL 后端 workspace 解析正确
- [x] 1.3 跑全量测试
  - 验证：593 个相关测试通过
- [ ] 1.4 web 层低风险点统一（knowledge_routes/skill_routes/run_archive/service_assistant_actions）
  - 说明：web 层 Windows 可解析 UNC，低风险，作为收尾

**阶段 1 完成标准**：WSL 后端进程消费任何平台视角路径都正确，无散落 expanduser 处理 config/task 路径。

## 阶段 2：offset 语义修复（根治串台，已决策）

目标：保证 interrupt/recover 时 offset 正确推进，共享 inbox + 过滤即可工作。

- [x] 2.1 `recover_stale_running_agent` 推进 offset（status.py）
  - 验证：test_recovered_stale_agent_advances_chat_offset 通过
- [x] 2.2 chat.py 主循环 host_native_path + worker_task_id 提前
  - 验证：测试通过
- [x] 2.3 评估 task 级 inbox 隔离，决定放弃（迁移风险高，共享 inbox + 过滤够用）
  - 记录：02-架构重构方案.md 附录 B

**阶段 2 完成标准**：interrupt/recover 后 offset 正确，多 task 并发 main 不串台（靠过滤 + offset）。

## 阶段 3：进程生命周期抽象（根治 interrupt/recover/offset）

目标：interrupt/recover/stop/start/offset 原子化。

- [x] 3.1 梳理 interrupt/recover 的 offset 推进逻辑，统一为原子操作
  - `services/agent_lifecycle.py::record_agent_interrupt`（advance offset + 标 interrupted + recovery_context 一步完成）
  - 新增 `chat_offsets.advance_chat_offset_to_inbox_end`，收敛 4 处重复
  - 验证：tests/test_agent_lifecycle.py 2 个 + interrupt/recover/offset 128 个通过
- [x] 3.2 后端进程生命周期（宿主 vs 工作进程）抽象
  - 说明：backend_status 已有 `_discover_backend_process`（/proc 扫 worker 命令）；本步新增 `_wait_for_worker_stop` —— stop_backend 的 pkill 是 fire-and-forget，重启会撞上仍持锁的孤儿 worker；现轮询 /proc 等 worker 真正退出（Windows 无 /proc 时 no-op）
  - 验证：test_wait_for_worker_stop_noops_when_no_proc + test_wait_for_worker_stop_polls_until_worker_gone
- [x] 3.3 全量回归
  - 验证：全量失败 15 个与干净基线一致（既有 env/环境问题），无新增回归

**阶段 3 完成标准**：interrupt/recover/stop 语义一致，无孤儿，无 offset 错位。

> **L0（阶段 3）已完成（2026-08-14）**：3.1 record_agent_interrupt 原子化（e857d71）+ 3.2 worker 退出等待（9c8a80f）+ 3.3 全量回归。

## L1：task 级 inbox 隔离（根治串台）【已完成 2026-08-14】

目标：inbox/offset 按 task 隔离，并发 main 任务不互相踩消息。

- [x] `inbox_path` 加可选 `task_id` → `inbox/{task_id}/{target}.jsonl`
- [x] 写端 `append_message`、读端 `agent_chat`/`chat_offsets`/`session_compact`/`chat_supervision`/`orchestrator.save_chat_offset` 统一传 task_id
- [x] run 级场景（auto_reply/协调）保持共享 inbox
- [x] 全量回归：17 失败 = 15 基线 + 2 既有 env，无新增
- [x] 提交 bd7f7f4

> 说明：原架构决策（02 附录 B）曾评估"放弃 inbox 隔离"，理由当时 offset 修复已够用。L1 现按新架构文档 §6.3 实施，物理隔离根治串台。

## 贯穿原则

1. 每层独立提交，可单独回滚
2. 每层完成跑全量测试
3. 涉及真实运行环境改动，先用临时任务验证
4. 不改前端 UI（除非必要）
5. 文档随进度更新
